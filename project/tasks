Issues:

- Cant use bootstrap on form with one id model generated by Django 

Solutions: 

- Create an template too Create/Add more items, once that way is raising error

- Download bootstrap to customize the forms created by django.





Issue:

- Create the urls folder to each app, and use the include() on the project urls